import { DynamicForm } from "@/shared/ui/DynamicForm/DynamicForm";
import { Box } from "@mui/material";

export function ResetPassword() {
  return (
    <Box p={5} bgcolor={"Background"}>
      <DynamicForm />
    </Box>
  );
}
